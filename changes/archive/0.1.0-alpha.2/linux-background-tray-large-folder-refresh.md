category: fix
issue: 11
pull: none
platforms: linux
user-facing: yes

Linux now uses the desktop StatusNotifier tray, keeps sync running after the window closes when enabled, launches through its supervised user service, and avoids repeated full refreshes for very large virtual folders.
