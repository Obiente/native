[package]
name = "nextcloud-native-runtime"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "A typed semantic compiler for adaptive native Nextcloud clients"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
url = "2.5"

[dev-dependencies]
pretty_assertions = "1.4"
