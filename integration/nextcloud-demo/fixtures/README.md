# NC Native E2E workspace

This file belongs to the disposable `nc-native-e2e` account. Tests may edit or
delete content under the `NC Native E2E` directory, but must not mutate data
outside that directory.
